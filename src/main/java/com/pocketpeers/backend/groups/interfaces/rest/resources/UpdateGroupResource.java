package com.pocketpeers.backend.groups.interfaces.rest.resources;

public record UpdateGroupResource(
                                   String name,
                                   String description) {
}
